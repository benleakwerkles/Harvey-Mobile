# FLOCK C16 — Harvey Mobile Resume Checkpoint

Repository: benleakwerkles/Harvey-Mobile
Branch: codex/harvey-mobile-vpg-20260719
Execution owner: CODEX_ROOT@MEDULLINA
Cloud-only: yes; no local Harvey source
Base gate: C15 head 30c2f0028374f60f2528633b765e890f28617d2e; run 30474157263; 73/73 contracts; typecheck, identity binding, Android and Web exports green.

Mission: add a deterministic, phone-first Resume Checkpoint derived only from the canonical Shift Handoff view, its identity-bound session, BuildIdentity, and injected time. Sections are COMPLETED_REVIEW, OPEN_ATTENTION, RESUME_HERE, GATE. The feature is advisory, session-only, transport NONE, read-only except model-owned session review/focus controls, and must never save, send, share, copy, execute, approve, merge, deploy, deliver, host, or write canon.

P rule: independently fetch both packets addressed to your Aeye group at the immutable C16 checkpoint root will provide; confirm exact path and blob for each before G. Do not rely on another Aeye's pull.
G rule: execute your two strongest ideas from your two packets, author implementation-grade code/tests, and return exact receipts plus predicted integration failures. Root alone reviews, integrates, cloud-pushes, and verifies.

Hard boundaries: preserve source objects byte-for-byte; reject unknown/raw/secret/effect-claim inputs without echo; retain truthful GATE on semantic blocks; invalid time/schema/effect/duplicate failures return zero derived items; IDs/order/bounds/effects are deterministic and recursively frozen; UI is accessible and 320px-safe; no network, filesystem, storage, clipboard, sharing, router, or deep-link APIs.

Addressed to DINK@MEDULLINA.

Contribution B: specify and implement an identity-bound Resume Checkpoint session with create/reset/review/advance functions. Review is idempotent and canonical, focus wraps across unreviewed checkpoint items, drift fails closed, effect flags remain all false, and Shift Handoff sources are never mutated. Provide exact Home wiring after ShiftHandoffCard and before CommandBoard.
