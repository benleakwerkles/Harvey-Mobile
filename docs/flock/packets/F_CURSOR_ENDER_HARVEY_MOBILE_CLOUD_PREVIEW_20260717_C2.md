# Flock packet — Cursor/Ender cloud-preview truth

Packet ID: `F_CURSOR_ENDER_HARVEY_MOBILE_CLOUD_PREVIEW_20260717_C2`
Status: `READY_FOR_PULL_INTERNAL_ROLE_AGENT`
Address: `CURSOR_ENDER_DOOZER@MEDULLINA`
Execution owner: `CODEX_ROOT`

## P — pull this state

- Repository: `benleakwerkles/Harvey-Mobile`
- Branch: `codex/harvey-mobile-vpg-20260717`
- Cycle-two base SHA: `0e332bab96d9829ed8d5ea1042038f532c27f490`
- Draft PR: `https://github.com/benleakwerkles/Harvey-Mobile/pull/2`
- External Ender/Claude is still `BLOCKED_UNBOUND`; this internal role packet must not be represented as external delivery.

## G — return the two strongest moves

1. Specify the smallest phone UI change that exposes bundle build identity without confusing it with the older project-state snapshot SHA. Include exact truth labels and unbound behavior.
2. Specify the strongest cloud-only visual-test handoff we can produce without deployment: a downloadable SHA-bound web export plus deterministic checks and receipt copy that says artifact, not live preview.

## Boundaries and receipt

- Build only in the Harvey-Mobile sandbox; no canon write, merge, or deployment.
- Never use Courtney repositories or folders.
- Receiver is read-only; `CODEX_ROOT` alone executes, commits, and pushes.
- Return `RECEIPTED_INTERNAL_ROLE_AGENT` with exactly two recommendations and evidence paths, or `BLOCKED` with one concrete blocker.
